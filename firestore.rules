service cloud.firestore {
  match /databases/{database}/documents {
    match /{collectionName}/{document=**} {
      allow read : if collectionName == "blog";
      allow read,write: if request.auth!=null;
    }
  }
}
